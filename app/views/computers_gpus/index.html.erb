<p id="notice"><%= notice %></p>

<h1>Computers Gpus</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @computers_gpus.each do |computers_gpu| %>
      <tr>
        <td><%= computers_gpu.name %></td>
        <td><%= computers_gpu.description %></td>
        <td><%= link_to 'Show', computers_gpu %></td>
        <td><%= link_to 'Edit', edit_computers_gpu_path(computers_gpu) %></td>
        <td><%= link_to 'Destroy', computers_gpu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Computers Gpu', new_computers_gpu_path %>
