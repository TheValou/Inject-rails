<%= form_with(model: computers_brand, local: true) do |form| %>
  <% if computers_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_brand.errors.count, "error") %> prohibited this computers_brand from being saved:</h2>

      <ul>
      <% computers_brand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :computers_brand_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_brand_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
