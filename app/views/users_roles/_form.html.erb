<%= form_with(model: users_role, local: true) do |form| %>
  <% if users_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_role.errors.count, "error") %> prohibited this users_role from being saved:</h2>

      <ul>
      <% users_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :users_role_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :users_role_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
