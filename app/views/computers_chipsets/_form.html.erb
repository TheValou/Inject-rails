<%= form_with(model: computers_chipset, local: true) do |form| %>
  <% if computers_chipset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_chipset.errors.count, "error") %> prohibited this computers_chipset from being saved:</h2>

      <ul>
      <% computers_chipset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :computers_chipset_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_chipset_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
