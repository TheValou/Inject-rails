<%= form_with(model: computers_screen, local: true) do |form| %>
  <% if computers_screen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_screen.errors.count, "error") %> prohibited this computers_screen from being saved:</h2>

      <ul>
      <% computers_screen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :computers_screen_type %>
  </div>

  <div class="field">
    <%= form.label :resolution %>
    <%= form.text_field :resolution, id: :computers_screen_resolution %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size, id: :computers_screen_size %>
  </div>

  <div class="field">
    <%= form.label :refresh_rate %>
    <%= form.number_field :refresh_rate, id: :computers_screen_refresh_rate %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_screen_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
