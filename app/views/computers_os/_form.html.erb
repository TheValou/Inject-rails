<%= form_with(model: computers_o, local: true) do |form| %>
  <% if computers_o.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_o.errors.count, "error") %> prohibited this computers_o from being saved:</h2>

      <ul>
      <% computers_o.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :computers_o_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_o_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
