<%= form_with(model: computers_activity, local: true) do |form| %>
  <% if computers_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_activity.errors.count, "error") %> prohibited this computers_activity from being saved:</h2>

      <ul>
      <% computers_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :computers_activity_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_activity_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
