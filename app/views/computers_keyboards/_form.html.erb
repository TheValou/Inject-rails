<%= form_with(model: computers_keyboard, local: true) do |form| %>
  <% if computers_keyboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computers_keyboard.errors.count, "error") %> prohibited this computers_keyboard from being saved:</h2>

      <ul>
      <% computers_keyboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :computers_keyboard_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :computers_keyboard_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
